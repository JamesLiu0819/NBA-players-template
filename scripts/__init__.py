# 用途：讓 scripts 資料夾變成一個 Python package,方便 server/app.py import
# 這裡面的 build_priority_items、format_dominant_factor_sentence、
# describe_growth_recommendation 等可重用計算函數,不用重寫一份。
# 可手動調整的變數：無,這個檔案本身是空的,不含任何邏輯。
