# 籃球球員模板分析系統 — 技術規格

版本 0.1 · 交接自對話設計階段

---

## 1. 背景與問題

原始流程是一套七步驟的 AI 對話式分析（問診 → 優劣勢對策 → 影片庫 → 自然位置判定 → 模板球員 → 訓練菜單 → 環境校準），已在單一使用者身上驗證有效，但無法通用化。三個斷點：

| 斷點 | 原因 |
|---|---|
| 自然位置判定 | 「位置」是離散標籤，無法計算距離。實際在做的是連續空間定位 |
| 模板球員配對 | 靠 LLM 記憶撈相似球員，結果不穩定、不可驗證 |
| 優先序排列 | 機會成本只寫在原則裡，沒有寫成公式，無法解釋為何 A 排在 B 前 |

另有架構缺陷：環境校準被放在第七步當作後處理，導致前六步全部返工。**它應該是第零步的權重。**

---

## 2. 架構：六層管線

```
L0 輸入層   問診 schema（固定欄位 + 規則式分支）
L1 特徵層   → 身體向量 B、技能向量 S、環境向量 E
L2 定位層   → 光譜座標 P、落差歸因（環境 vs 能力）
L3 匹配層   → 模板球員 = P 空間中的加權最近鄰
L4 處方層   → 優先序評分函數 → 排序後的改善項目清單
L5 輸出層   → 影片索引、訓練菜單、比賽日清單（由 L4 的 tag 驅動查表）
```

關鍵差異：原流程是七份依序產出的文件；新架構是**四個資料物件 + 兩個渲染層**。L5 不做分析，只做查表與渲染。

---

## 3. 資料模型

### 3.1 光譜座標（四軸，各 0–100）

取代 1–5 號位置標籤。位置從分類問題變成座標問題，相似度變成可計算的距離。

| 軸 | 0 端 | 100 端 | 可習得性 |
|---|---|---|---|
| A 持球創造 | 純終結者 | 主要持球核心 | 高 |
| B 空間位置 | 禁區作業 | 外線作業 | 中 |
| C 防守對位 | 護框型 | 追防外圍型 | 中 |
| D 運動能力層級 | 地板型 | 頂級運動能力 | **低（視為固定）** |

D 軸標為不可習得是刻意設計，讓兩件事自動化：

- **差異欄位**：`差異 = argmax(模板球員座標 − 使用者座標)`，取差距最大的軸自動生成。
- **反面對照**：形式化為「A/B/C 軸接近但 D 軸差距最大」的球員 — 看起來像你，但靠的是你沒有的東西。

**落差歸因**：問診同時記錄「球隊實際擔任位置座標」與「自然位置座標」，向量差即落差。差在 A/B/C → 環境問題（低層級聯賽缺人被迫打不適合位置）；差在 D → 能力天花板。

### 3.2 環境向量 E

五維度 × 3 檔，枚舉組合。每個組合對應一張技能收益倍率表 `E_i ∈ [0.5, 2.0]`。

| 維度 | 檔位 |
|---|---|
| 防守收縮程度 | 內縮 / 正常 / 外擴 |
| 換防頻率 | 低 / 中 / 高 |
| 節奏 | 慢 / 中 / 快 |
| 對抗吹判尺度 | 鬆 / 中 / 嚴 |
| 平均身體條件層級 | 低 / 中 / 高 |

範例（防守內縮 + 吹判鬆）：

| 技能 | E_i |
|---|---|
| 三分 / 中距離跳投 | 1.8 |
| 面框第一步過人 | 1.4 |
| 高位傳導視野 | 1.5 |
| 背框單打 | 0.6 |

完整倍率表需人工填寫，存於 `data/env_weights.json`。243 種組合不必窮舉：用五個獨立的乘數表相乘即可（`E_i = Π(dim_multiplier)`），實作上只需 5 × 3 × 技能數 的表格。

### 3.3 技能庫

約 15 項核心技能，每項欄位：

| 欄位 | 說明 |
|---|---|
| `id` / `name_zh` | 識別 |
| `category` | offense / defense / awareness |
| `cost_C` | 習得成本 1–5（時間 × 場地依賴 × 隊友依賴） |
| `court_required` | bool → 決定進「球場環節」或「場外環節」 |
| `teammate_required` | bool |
| `axis_relevance` | 對 A/B/C/D 四軸的相關性向量，用於算 R_i |
| `video_tags` | 對應影片庫標籤 |
| `metric` | 驗收指標（見 §7） |

### 3.4 球員庫

100–150 名現役球員，欄位 = 四軸座標 + 身體數據 + `learnability_flag`。建置方式見 §6。

---

## 4. 優先序評分函數

把「機會成本才是真正的決策依據」這條原則寫成公式：

```
P_i = (G_i × E_i × R_i) / C_i
```

| 符號 | 意義 | 範圍 | 來源 |
|---|---|---|---|
| `G_i` | 缺口 = 目標水準 − 現況 | 0–5 | L0 問診 |
| `E_i` | 環境收益倍率 | 0.5–2.0 | 環境向量查表 |
| `R_i` | 對自然位置的相關性 | 0–1 | L2 座標 × `axis_relevance` |
| `C_i` | 習得成本 | 1–5 | 技能庫固定值 |

三個附帶產出：

1. 排序自動化。
2. **取捨理由自動生成** — 「為什麼不做 B」的答案就是 `P_A > P_B` 以及是哪個因子拉開的（E 太低？C 太高？）。輸出時要把主導因子印出來。
3. 訓練時間分配 = 背包問題：在使用者填寫的每週總時數上限下最大化 `Σ P_i`。

---

## 5. 問診系統（L0）

### 5.1 核心陷阱：參照群體效應

自評量表的分數被受測者的參照群體錨定。D4 球員的「三分能力 4 分」與 D1 球員的「4 分」差兩個層級，跨使用者不可比。而 `G_i` 直接由自評決定，誤差會污染整個排序。

**解法：不問能力，問行為頻率與具體結果。**（BARS，行為錨定評分量表）

| ❌ 錯誤問法 | ✅ 正確問法 |
|---|---|
| 你的三分能力如何？1–5 | 當防守者退到罰球線放你投三分時，你會出手嗎？<br>1 從不 / 2 很少 / 3 一半 / 4 常常 / 5 一定 |
| 你的傳導能力如何？1–5 | 高位接球後被包夾，你的第一反應是？<br>1 一定失誤 / 2 常勉強運球 / 3 能傳出但慢 / 4 能傳到空檔 / 5 能傳到下一個空檔 |
| 你的護框能力如何？1–5 | 上一場有幾次對手殺進禁區你有正面對位？其中幾次守下來？（數值） |

行為題不依賴受測者對「好」的定義，因此可跨層級比較。**這是 P1 能否成立的核心動作。**

### 5.2 四種輸入型態

不要強行統一成 1–5。

| 原範疇 | 輸入型態 |
|---|---|
| 身材 | **數值**：身高 / 體重 / 臂展 / 立定摸高 / 助跑摸高 / 20m 衝刺 |
| 場上狀態、技能 | **行為型 Likert**（1–5） |
| 角色定位 | **單選枚舉** |
| 目標方向 | **單選 + 排序** |
| 限制 | **數值 + 多選**：每週時數、有無球場、有無隊友 → 餵給 C_i |
| 聯賽防守環境 | **情境單選**（5 維 × 3 檔） |
| 主要困擾 | **多選（限選 3）** → 用於交叉驗證 |

### 5.3 三道驗證機制（取代 AI 的判斷力）

| 機制 | 規則 | 抓什麼 |
|---|---|---|
| 一致性檢查 | 身高 < 180 且護框自評 ≥ 4 → 觸發覆核題 | 自評膨脹 |
| 反向題 | 同構念正反各問一次，差距 > 2 → 降低該項權重 | 亂填 / 誤解題意 |
| 困擾—分數交叉比對 | 困擾選了傳導但傳導自評 4 → 觸發追問 | 自我認知矛盾 |

### 5.4 規則式分支（決定樹，非 AI）

```
if 身高 ≥ 195           → 開啟護框題組(4)，關閉追防外圍題組
if 每週時數 < 3          → 略過長期發展題組，C_i 門檻收緊
if 環境 = 防守內縮       → 開啟外線投射題組(6)
```

複製原流程「一次一題、條件分支」的體驗，UI 為單題單頁。

### 5.5 題數預算

每個構念至少 3 題以維持信度（Cronbach's α）。

| 區塊 | 題數 |
|---|---|
| 身材（數值） | 6 |
| 四軸定位（A/B/C/D 各 4） | 16 |
| 技能向量（15 項 × 1–2，經分支篩選） | 12–18 |
| 聯賽環境 | 5 |
| 目標 / 限制 / 困擾 | 6 |
| **合計** | **45–51 題，7–9 分鐘** |

---

## 6. 球員資料庫建置

### 6.1 逐軸資料可得性

| 軸 | 可得性 | 來源欄位 |
|---|---|---|
| A 持球創造 | 🟢 高 | Usage Rate、AST%、Time of Possession、Touches、Drives |
| B 空間位置 | 🟢 高 | 3PA rate、禁區出手佔比、平均出手距離、shot chart |
| C 防守對位 | 🟡 中 | BLK%、DREB%、matchup difficulty、對位球員平均身高 |
| D 運動能力 | 🔴 低 | Draft Combine 有立定摸高/衝刺，但大量球員未測、且與現況脫節 |

D 軸恰好是最關鍵的一軸（反面對照的判定依據），也是唯一抓不到資料的。

### 6.2 混合式策略

| 軸 | 方式 | 工作量 |
|---|---|---|
| A / B | 統計推導 → 標準化到 0–100 | 一次性腳本 |
| C | 統計推導 + 抽查校正 | 腳本 + 半天 |
| D | **人工標註 3 檔**（地板 / 平均 / 精英） | 120 人 × 10 秒 ≈ 半天 |

D 軸只需 3 檔就足以支撐反面對照判定，不需連續值。之後每年只增補新秀。

順帶標註 `learnability_flag`：該球員賴以成功的核心特質是否可複製。這讓反面對照從「D 軸差很遠」升級成「核心優勢不可複製」，且只有人標得出來。

### 6.3 資料來源與注意事項

| 來源 | 現況 | 建議 |
|---|---|---|
| `nba_api` (Python) | 仍在維護，1.4.x。包裝 NBA.com 上大量未公開文件、頻繁變動的 API | 首選，但要有 endpoint 壞掉的心理準備 |
| stats.nba.com 直連 | 請求量大時會封鎖，雲端 IP 尤其容易被擋 | 本機跑，不要放伺服器即時查 |
| Basketball Reference | Sports Reference 有每分鐘 20 次請求的速率限制 | 加延遲、遵守 robots.txt 與使用條款 |

### 6.4 建庫流程

```
離線批次（每季一次或每月更新）
  → 產出靜態 players.json（約 200KB）
  → 網站直接載入，最近鄰查詢在前端算
```

同時解決速率限制、成本、延遲、使用條款四個問題。**執行期不碰任何外部 API。**

---

## 7. 訓練菜單與驗收指標

原則：「改善傳導」無法驗收，「30 次中失誤 ≤ 2 次」可以。

技能庫的 `metric` 欄位：

| 欄位 | 範例 |
|---|---|
| 量測動作 | 高位接球後轉身傳導 |
| 分母 | 30 次 |
| 通過門檻（分層） | 入門 ≤5 失誤 / 進階 ≤2 / 精熟 ≤1 |
| 場地需求 | 需球場 / 不需 |
| 隊友需求 | 需 / 不需 |

後兩欄直接驅動「球場環節 vs 場外環節」的拆分，不需再判斷一次。

---

## 8. 影片庫

從「每次即時搜尋」改成「標籤索引」。

| 原做法 | 改成 |
|---|---|
| 針對使用者即時搜尋 3 部 | 40–60 個技能標籤的策展影片庫，每標籤 5–8 部，約 300 部 |
| LLM 生成「該看的重點」 | 入庫時寫一次，所有使用者共用 |
| 橘/藍/紫/綠色彩系統 | 保留，改為標籤 metadata，渲染時自動上色 |

色彩系統：**橘 = 進攻 · 藍 = 防守 · 紫 = 模板 · 綠 = 訓練**

L4 輸出排序後的技能標籤，L5 拿標籤查表。連結永遠真實可用，邊際成本趨近零。需要一個 link-checker 定期跑失效偵測。

---

## 9. 最終輸出規格（七項）

沿用原流程的產出，但全部由前述資料物件驅動：

1. **球員檔案表**（十欄）
2. **意識提升 / 實際操作** 兩份對策，進攻防守分開
3. **影片資料庫**：每部註明創作者與該看的重點，套用色彩系統
4. **自然位置判定** + 3 位深度模板（技能最貼合 / 身體最貼合 / 天花板方向）+ 10 人現役對照表 + 反面對照，每位附 2–3 部影片
5. **訓練菜單**：球場環節（時間 / 區塊 / 內容 / 可量化成功標準）+ 場外環節（頻率 / 區塊 / 內容 / 解決哪個問題）
6. **比賽日執行清單**：只有 5 條，格式「什麼時機 → 做什麼」
7. **環境校準說明表**（項目 / 未加權排序 / 加權後排序 / 理由）— 注意：不再是「修正表」，而是展示 E 權重造成的差異

### 10 人對照表欄位

| 欄位 | 要求 |
|---|---|
| 排序 | 依貼合度（座標距離）排 |
| 球員 | 姓名 + 所屬球隊 |
| 相似處 | 具體到技能與身體條件，不寫空泛形容 |
| 差異 | **必填**。由 argmax 軸自動生成 + 文案潤飾。只列相似處會讓人學不該學的東西 |
| 最值得學的一件事 | 只能一件，且必須可執行 |
| 貼合度 | 五星制 |

表格後接：角色原型總結 + 「下限 / 中位數 / 上限」三層路線圖 + 反面對照段落（不該以誰為模板、那方向會誘導人走錯什麼路）。

---

## 10. 開發階段

| 階段 | 範圍 | 驗收 |
|---|---|---|
| **P1** | 問診 schema + 四軸座標 + 優先序公式，輸出純文字報告 | 黃金案例回歸測試通過 |
| **P2** | 球員特徵資料庫（80–150 人，含 D 軸與 learnability_flag），模板配對改為最近鄰查詢 | 結果穩定可重現，反面對照自動生成 |
| **P3** | 影片標籤庫 + 訓練菜單模板庫 + HTML 渲染（含色彩系統） | 完整取代現行流程 |
| **P4** | 回饋迴路：使用者回報驗收數據 → 更新 G_i → 重排優先序 | 從一次性分析變成持續系統 |

P1 先做，用來驗證整套量化邏輯是否成立。

---

## 11. 黃金測試案例

`tests/fixtures/james.json` — 系統設計者本人，已有手工分析的已知結論：

- 實際擔任位置：中鋒（雪梨 D4 聯賽）
- 手工判定的自然位置：非中鋒；模板為 Amen Thompson / Derrick Jones Jr. / Kirilenko 方向
- 聯賽環境特徵：防守普遍內縮
- 已知的環境校準結果：原判定為「已具備、維持即可」的技能，加權後應升為第一優先

**驗收標準**：engine 對此輸入產出的四軸座標、落差歸因（應歸因於環境而非能力）、以及加權後的優先序前三名，須與上述結論一致。不一致代表公式權重需要校準，而非測試錯誤。

---

## 12. 未決問題

| # | 問題 | 誰決定 | 是否阻塞 |
|---|---|---|---|
| 1 | 行為錨點撰寫範圍：15 項技能全寫完（45–60 個錨點敘述）再開發，還是先取 5 項關鍵技能（外線投射、面框第一步、高位傳導、護框、換防跟防）做最小可跑版本驗證公式權重 | 產品 | **阻塞 P1** |
| 2 | 環境倍率表的初始值由誰填、依據什麼 | 領域專家 | 阻塞 P1 |
| 3 | 自評分數是否要做聯賽層級的組內標準化（需累積足夠使用者才可行）；早期先用專家設定的錨點 | 資料 | 不阻塞 |
| 4 | 前端技術棧 | 工程 | 不阻塞 |
| 5 | 是否需要使用者帳號與歷史紀錄（P4 前不需要） | 產品 | 不阻塞 |

---

## 13. 非目標（v1 明確不做）

- 影片自動分析 / 姿勢辨識 — 另一個量級的專案
- 即時比賽數據串接 — 使用者端無此資料來源
- 社群 / 比較排行 — 會誘發參照群體效應，與 §5.1 的設計目標衝突
- 非 NBA 球員模板庫（大學、歐洲聯賽）— 資料可得性差，v2 再議
- 中文以外語言介面
